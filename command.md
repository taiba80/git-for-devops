## 📁 Basic Linux Commands
ls
pwd
cd taiba
cd ..
cd kamal
clear
mkdir git_for_devops
touch dosto.txt
touch hello.txt
touch anosh.txt
rm taiba.txt
cat taiba.txt
cat dosto.txt
vim taiba.txt
vim dosto.txt
vim hello.txt
ls -a

---

### 🛠️ Git Initialization & Configuration
git init
git config --global user.name "taibakamal80"
git config --global user.email "taibakamal@gmail.comm"

---

### 🛆 Git Staging & Committing
git status
git add dosto.txt
git add hello.txt
git add anosh.txt
git commit -m "adding dosto hello"
git commit -m "added changes to dosto"
git commit -m "added changes to hello"
git commit -m "add anosh"

---

### 📀 Git Branching & Switching
git branch
git checkout -b dev
git checkout master
git checkout dev

---

### 📜 Git Logs
git log
git log --oneline
git og  # (Note: likely a typo of `git log`)

---

### 🧩 Miscellaneous
jenkins-plugin-cli --plugins git:5.6.0
history
